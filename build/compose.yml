services:
  ubuntu:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../test:/test
    command: tail -f /dev/null

  alpine:
    build:
      context: .
      dockerfile: Dockerfile.alpine
    volumes:
      - ../test:/test
    command: tail -f /dev/null